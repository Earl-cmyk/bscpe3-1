const $ = (selector) => document.querySelector(selector);
const money = (value) => `PHP ${Number(value).toFixed(2)}`;
async function loadBudget() { const response = await fetch('/api/budget'); const data = await response.json(); if (!response.ok) throw new Error(data.error || 'Unable to load budget'); const deposits = data.entries.filter((entry) => entry.type === 'deposit' && entry.status !== 'cancelled').reduce((sum, entry) => sum + Number(entry.amount), 0); const withdrawals = data.entries.filter((entry) => entry.type === 'withdraw' && entry.status !== 'cancelled').reduce((sum, entry) => sum + Number(entry.amount), 0); const total = deposits + withdrawals; const depositPercent = total ? (deposits / total) * 100 : 0; const circumference = 100; $('#balanceValue').textContent = money(data.balance); $('#entryCount').textContent = `${data.entries.length} entries`; $('#pieLabel').textContent = total ? `${Math.round(depositPercent)}% in` : 'No entries'; $('#pieDeposit').style.strokeDasharray = `${depositPercent} ${circumference - depositPercent}`; $('#pieWithdraw').style.strokeDasharray = `${100 - depositPercent} ${depositPercent}`; $('#pieWithdraw').style.strokeDashoffset = `${-depositPercent}`; $('#budgetEntries').innerHTML = data.entries.map((entry) => `<div class="budget-entry"><span class="entry-icon ${entry.type}">${entry.type === 'deposit' ? '+' : '-'}</span><span><strong>${entry.reason}</strong><small>${entry.type} - ${entry.status}</small></span><b>${entry.type === 'deposit' ? '+' : '-'}${money(entry.amount)}</b>${entry.type === 'withdraw' && entry.status === 'pending' ? `<span class="entry-actions"><button class="button button-quiet" data-action="cancel" data-id="${entry.id}" aria-label="Cancel withdrawal">&times;</button><button class="button button-primary" data-action="spent" data-id="${entry.id}" aria-label="Mark spent">&check;</button></span>` : ''}</div>`).join('') || '<p class="muted">No entries yet.</p>'; }
$('#openAudit').onclick = () => { $('#pinInput').value = ''; $('#pinError').hidden = true; $('#pinModal').hidden = false; };
$('#pinForm').onsubmit = async (event) => { event.preventDefault(); const response = await fetch('/api/verify-pin', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ pin: $('#pinInput').value }) }); if (!response.ok) { $('#pinError').textContent = 'Invalid PIN'; $('#pinError').hidden = false; return; } $('#auditForm').dataset.pin = $('#pinInput').value; $('#pinModal').hidden = true; $('#auditModal').hidden = false; };
document.querySelectorAll('[data-close]').forEach((button) => { button.onclick = () => { document.querySelector(`#${button.dataset.close}`).hidden = true; }; });
$('#auditForm').onsubmit = async (event) => { event.preventDefault(); const payload = Object.fromEntries(new FormData(event.currentTarget)); payload.pin = event.currentTarget.dataset.pin || ''; const response = await fetch('/api/budget', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(payload) }); const data = await response.json(); if (!response.ok) { $('#auditError').textContent = data.error; $('#auditError').hidden = false; return; } $('#auditModal').hidden = true; event.currentTarget.reset(); await loadBudget(); };
$('#budgetEntries').onclick = async (event) => { const button = event.target.closest('[data-action]'); if (!button) return; const pin = window.prompt('Enter your PIN'); if (pin === null) return; const response = await fetch(`/api/budget/${button.dataset.id}`, { method: 'PATCH', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ action: button.dataset.action, pin }) }); const data = await response.json(); if (!response.ok) window.alert(data.error); else await loadBudget(); };
loadBudget().catch((error) => { $('#budgetEntries').innerHTML = `<p class="form-error">${error.message}</p>`; });
